[build]
  # Skip Netlify rebuild unless index.html itself changed.
  # Score updates only touch data.json, which is now fetched directly
  # from the GitHub raw CDN — no rebuild needed for those.
  ignore = "git diff --quiet HEAD^ HEAD -- index.html"
